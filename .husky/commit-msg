#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# 커밋 메시지 파일을 읽어옴
message="$(cat "$1")"

# 정규식: #(숫자) 패턴이 있는지 검사
if ! echo "$message" | grep -qE "#[0-9]+"; then
  echo "❌ [커밋 차단] 커밋 메시지에 이슈 번호가 없습니다!"
  echo "👉 예시: feat: 헤더 UI 구현 (#1)"
  exit 1
fi